# MLC Architecture site

This static preview will be migrated to **WordPress on DreamHost**. Every change
must be portable to a WordPress theme. Read the "Destination: WordPress on
DreamHost" section of `README.md` before making changes, and flag anything that
wouldn't migrate cleanly instead of building it.
